-- database: ../runtime/db/starwars.db

-- The first line (above) must be present in all SQL script (.sql) files.
-- It is used by the VSCode SQLite extension(s) to locate the database
-- file. SQL statements follow. For example:

SELECT * FROM characters;
